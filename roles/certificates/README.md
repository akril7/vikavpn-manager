Install certificate

-----------------------------------------------------------
Vars

certificates:
*required:*
  domain
*default*
  local_dir
  remote_dir
  cert_file 
  key_file

certificates_paths:
  cert
  key
